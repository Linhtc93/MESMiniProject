# Manufacturing ERP Client (React + Vite + Material UI)

## Scripts
- npm install
- npm run dev

## Env
Copy `.env.example` to `.env` and adjust `VITE_API_URL` if your backend runs elsewhere.

Default backend: http://localhost:4000/api
