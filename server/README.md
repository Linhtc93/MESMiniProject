# Manufacturing ERP Server (Node.js + Express + MongoDB)

## Scripts
- npm install
- npm run dev
- npm run seed

## Env
Copy `.env.example` to `.env` and adjust values.

## API Prefix
All routes are prefixed with `/api`.
